# MonoLisa

MonoLisa 3.000, the `MonoLisaCode` variable webfont, roman and italic.

© 2026 FaceType Foundry. All Rights Reserved.

**This font is not open source.** It is subject to a EULA, and the copy in
`woff2/` is the licensed webfont package as the foundry delivers it — the
self-hosting form, which is what a web licence is for. The terms are at
<https://www.monolisa.dev/license>; a user licence can be acquired at
<https://www.monolisa.dev>.

`monolisa-code.css` is the foundry's own stylesheet, vendored verbatim,
including the `/*! @preserve */` header that carries this notice into
anything that processes it. `assets/fonts.py` rewrites its `src` urls when
it renders a page and changes nothing else.
