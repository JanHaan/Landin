# Landin for JetBrains IDEs

Enable the bundled **TextMate Bundles** plugin, open its settings, add the
`../textmate` directory as a bundle, and map `*.ldn` if the IDE does not pick
up the extension automatically. This reuses the checked grammar rather than
maintaining a second JetBrains lexer.
