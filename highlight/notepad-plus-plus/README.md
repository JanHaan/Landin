# Landin for Notepad++

Import `Landin.xml` with **Language → User Defined Language → Define your
language → Import**, or copy it into `%AppData%\Notepad++\userDefineLangs` and
restart Notepad++. The generated UDL associates `.ldn` and covers comments,
literals, operators, keywords, built-in types, constants, and folding words.
