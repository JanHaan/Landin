# Merge with the existing host's compose.yaml; do not replace its services,
# credentials, image or named volumes. Equal RAM and RAM+swap limits disable
# swap in Docker. The runner independently verifies the kernel cgroup values.
services:
  runner:
    mem_limit: 107374182400
    memswap_limit: 107374182400
