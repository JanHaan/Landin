consume: (value: i32) -> none =
    _ = value
end consume

fills_return: () -> (result: i32) =
    defer consume(begin
        result = 42
        result
    end)
    return
end fills_return

assigned_later: () -> none =
    mut local: i32
    defer consume(local)
    local = 42
end assigned_later
