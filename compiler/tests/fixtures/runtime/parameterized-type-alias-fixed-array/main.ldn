bytes: type (t: type, fixed n: u64) = [n]t
mut data: bytes(u8, 4)

public main: () -> (code: i32) =
    local: bytes(u16, 6)
    if lenof data == 4 and lenof local == 6 then
        code = 42
    else
        code = 1
    end if
end main
