problem: atom
leaf: () -> none ! ... = fail problem end leaf
outer: () -> none ! ... =
    leaf() else (error)
        alias := error
        fail alias
    end
end outer
public main: () -> (code: i32) =
    code = 1
    outer() else (error) _ = error code = 42 end
end main
