import core/text

public main: () -> (code: i32) =
    source: utf8 = "A"
    mut dummy: [1]u8 = [0]
    zero := text.first(dummy[0..<1])
    end_position := text.next(zero)
    invalid: []u8 = source[end_position]
    code = i32(invalid[0])
end main
