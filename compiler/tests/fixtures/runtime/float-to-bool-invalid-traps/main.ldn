to_bool: (value: f64) -> (result: bool) =
    result = bool(value)
end to_bool

public main: () -> (code: i32) =
    if to_bool(f64.nan) then
        code = 1
    else
        code = 2
    end if
end main
