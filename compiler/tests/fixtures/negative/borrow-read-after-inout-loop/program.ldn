increment: (inout value: u32) -> none =
    value = value + 1
end increment

bad: () -> none =
    mut value: u32 = 1
    view: ptr u32 = addr value
    loop do
        increment(value)
        break
    end loop
    _ = view.val
end bad
