--  D46 gives this type module storage, but its local frame representation
--  still waits for nested-place, initialization and whole-copy rules.
holder: type = struct
    row: [4]u8
    count: u32
end holder

f: () -> none =
    mut here: holder
end f
