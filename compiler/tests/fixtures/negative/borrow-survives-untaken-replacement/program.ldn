increment: (inout value: u32) -> none =
    value = value + 1
end increment

bad: () -> none =
    mut value: u32 = 1
    mut view: ptr u32 = addr value
    increment(value)
    if false then
        view = addr value
    end if
    _ = view.val
end bad
