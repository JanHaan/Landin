anchor: i32 = 0
mut saved: ptr i32 = addr anchor

retain: (escaping value: ptr i32) -> (yes: bool) =
    saved = value
    yes = true
end retain

bad: () -> none =
    local: i32 = 42
    while retain(addr local) do
        break
    end while
end bad
