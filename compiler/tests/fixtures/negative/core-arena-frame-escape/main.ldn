import core/mem

bad: () -> (state: mem.arena) =
    mut bytes: [32]u8 = zeroed
    base: ptr mut u8 = addr bytes[0]
    extent: usize = 32
    state = mem.arena_over(base, extent)
end bad

public main: () -> (code: i32) =
    _ = bad()
    code = 0
end main

