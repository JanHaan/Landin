size: () -> (r: u32) = r = 1 end size

mut n: u32

--  [1820] indexes what a selection named, so a call and a parenthesised
--  expression are each their own production and neither is indexed.
f: () -> none =
    a := size()[0]
    b := (n)[0]
end f
