choice: type = struct
    kind: variant
        leaf |
        row: (values: [2]u8)
    end kind
end choice

mut state: choice

bad: () -> none =
    match state.kind
        leaf: _ = 0
        row(values): values[0] = 1
    end match
end bad
