absent: atom
maybe_pointer: type = absent | ptr u32
stored: u32 = 42
mut shared: maybe_pointer = absent

replace: () -> none =
    shared = addr stored
end replace

bad: (cell: ptr u32) -> (result: maybe_pointer from cell) =
    shared = absent
    replace()
    result = shared
end bad
