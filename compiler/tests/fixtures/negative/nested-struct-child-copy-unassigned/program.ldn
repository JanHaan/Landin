inner: type = struct
    value: i32
end inner

outer: type = struct
    nested: inner
end outer

copy: () -> none =
    mut source: outer
    mut destination: outer = zeroed
    destination.nested = source.nested
end copy
