holder: type = struct
    kind: variant
        dot |
        box: (w: u8)
    end kind
end holder

--  R7.20: a case is written where its variant part is the destination.
f: () -> none =
    mut h: holder = zeroed
    h.kind = dot
    alone := dot
end f
