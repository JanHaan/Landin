row: type = [4]u8

--  A declaration-only local has frame storage now, while the whole array
--  value still waits for copying and initialization in a later R2.20 slice.
f: () -> none =
    mut here: row
    _ = here
end f
