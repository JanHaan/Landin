row: type = [4]u8

--  A declaration-only local has frame storage now, while a bare whole-array
--  read remains outside the contextual copy and initializer forms.
f: () -> none =
    mut here: row
    _ = here
end f
