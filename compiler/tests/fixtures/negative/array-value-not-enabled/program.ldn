row: type = [4]u8

--  A declaration-only local has frame storage, and [1930] discards a whole
--  array where it stands -- which still reads it, before any assignment.
f: () -> none =
    mut here: row
    _ = here
end f
