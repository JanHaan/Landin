mut words: [4]u32

--  [1820] spells `selection ("[" expression "]")*`, so the brackets come
--  after the whole selection and nothing derives a dot after one: a field
--  of an element is a struct inside an array, which the layout cannot
--  hold yet either.
f: () -> (r: u32) =
    r = words[0].x
end f
