absent: atom
maybe_pointer: type = absent | ptr u32
stored: u32 = 42

bad: (cell: ptr u32, missing: bool)
     -> (result: maybe_pointer from cell) =
    result = if missing then absent else addr stored end if
end bad
