mut words: [4]u32

--  [1950]: an index the compiler knows is refused where it cannot be
--  taken, and a negative one is outside every length.
f: () -> (r: u32) =
    r = words[4]
end f

g: () -> (r: u32) =
    r = words[-1]
end g
