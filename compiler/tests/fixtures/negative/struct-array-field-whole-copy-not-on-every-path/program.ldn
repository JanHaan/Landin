holder: type = struct
    tag: u8
    row: [2]u32
end holder

copy: (condition: bool) -> none =
    mut source: holder
    source.tag = 1
    if condition then
        source.row = zeroed
    end if
    mut destination: holder
    destination = source
end copy
