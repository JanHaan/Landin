holder: type = struct
    pointer: ptr u32
end holder

consume: (sink pointer: ptr u32) -> none =
    _ = pointer
end consume

bad: (inout value: holder) -> none =
    loop do
        consume(value.pointer)
        break
    end loop
end bad
