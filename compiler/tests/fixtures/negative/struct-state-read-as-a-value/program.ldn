counters: type = struct
    hits: u32
end counters

mut state: counters

--  [1740]'s state of a struct type is storage a program may declare, and
--  [1930] may discard the whole of it, but a whole struct is not a scalar.
f: () -> none =
    _ = state
    total: u32 = state
end f
