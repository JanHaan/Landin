marker: type = concept (item: type)
end marker

replace: (items: []any marker) -> none =
    for item in items do
        item = item
    end for
end replace

public main: () -> (code: i32) =
    code = 1
end main
