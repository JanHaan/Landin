import core/heap
import core/mem
filter: type = struct
    needle: utf8
end filter
bad: (inout state: heap.system, source: utf8)
     -> (result: ptr mut filter) ! mem.out_of_memory =
    initial: filter = (needle: source)
    result = try mem.new(state, initial)
end bad
