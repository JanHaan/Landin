inner: type = struct
    q: u32
end inner

--  D87 gives the containing type zeroed runtime storage.  The nested field
--  itself is still not a general aggregate value.
outer: type = struct
    part: inner
    count: u32
end outer

mut here: outer

f: () -> none =
    _ = here.part
end f
