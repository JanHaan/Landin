consume: (sink value: u32) -> none =
    _ = value
end consume

bad: () -> none =
    mut value: u32 = 42
    mut i: usize = 0
    while i < 2 do
        consume(value)
        inc i
    end while
end bad
