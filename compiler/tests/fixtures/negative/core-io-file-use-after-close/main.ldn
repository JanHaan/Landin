import core/io

bad: (inout state: io.system, opened: io.file)
     -> none ! io.io_failed =
    mut byte: [1]u8 = zeroed
    try io.close(state, opened)
    _ = try io.read(state, opened, byte[0..<1])
end bad
