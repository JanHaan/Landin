site: type = struct
    file_id: u32
    line: u32
    column: u32
end site

ordinary: (where: site) -> none =
    _ = where.line
end ordinary

accept: (callback: (caller where: site) -> none) -> none =
    callback()
end accept

bad: () -> none =
    accept(ordinary)
end bad
