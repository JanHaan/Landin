absent: atom
maybe_pointer: type = absent | ptr u32

consume: (value: i32) -> none =
    _ = value
end consume

bad: (cell: ptr u32, choose: bool) -> (result: maybe_pointer from cell) =
    local: u32 = 42
    mut candidate: maybe_pointer = addr local
    result = cell
    defer consume(begin
        candidate = absent
        0
    end)
    return when choose
    result = candidate
end bad
