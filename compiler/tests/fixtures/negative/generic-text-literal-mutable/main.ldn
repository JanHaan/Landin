identity: (item: type, value: item, text: []mut u8) -> (answer: item) =
    _ = text
    answer = value
end identity

public main: () -> (code: i32) =
    code = 0
    bad := identity(42, "hello")
end main
