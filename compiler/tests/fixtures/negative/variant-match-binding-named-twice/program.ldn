choice: type = struct
    kind: variant
        leaf |
        pair: (first: u8, second: u8)
    end kind
end choice

mut state: choice

f: () -> none =
    match state.kind
        leaf: _ = 0
        pair(value, value): _ = value
    end match
end f
