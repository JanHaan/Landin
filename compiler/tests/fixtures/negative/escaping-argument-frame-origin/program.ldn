anchor: i32 = 0
mut saved: ptr i32 = ptr(0x1000)

retain: (escaping value: ptr i32) -> (yes: bool) =
    saved = value
    yes = true
end retain

bad: () -> none =
    local: i32 = 42
    _ = retain(addr local)
end bad
