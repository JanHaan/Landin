anchor: i32 = 0
mut saved: ptr i32 = ptr(0x1000)

retain: (escaping value: ptr i32) -> (yes: bool) =
    saved = value
    yes = true
end retain

bad: () -> none =
    local: i32 = 42
    if retain(addr local) then
        _ = 1
    end if
end bad
