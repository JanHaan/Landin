-- Generated by devices/generate.py from the pinned RP2040 SVD.
-- Copyright (c) 2024 Raspberry Pi Ltd. BSD-3-Clause.
-- See devices/inputs/LICENSE.pico-sdk and devices/README.md.
-- Reset metadata performs no initialization. No device RMW is implicit.
compiler.assert(compiler.byte_order == little)
compiler.assert(sizeof u32 == 4 and alignof u32 == 4)
public base: usize = 0x40034000

public uartdr_offset: usize = 0x0
public uartdr_address: usize = 0x40034000
public uartdr_named_mask: u32 = 0x00000fff
public uartdr_reset_mask: u32 = 0x00000000
public uartdr_reset_value: u32 = 0x00000000
public uartdr_image: type = layout(packed, u32) struct
    f_oe: u1 at 11..11
    f_be: u1 at 10..10
    f_pe: u1 at 9..9
    f_fe: u1 at 8..8
    f_data: u8 at 0..7
end uartdr_image

public uartdr_decode: (raw: u32) -> (image: uartdr_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end uartdr_decode

public uartdr_encode: (image: uartdr_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end uartdr_encode

public uartdr_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + uartdr_offset)
    raw = compiler.volatile_load(port)
end uartdr_read

public uartdr_write: (device_base: usize, raw: u32) -> none =
    port: ptr mut u32 = ptr(device_base + uartdr_offset)
    compiler.register_write(port, raw, compiler.normal_write,
                            compiler.write_zero, 0x000000ff)
end uartdr_write

public uartrsr_offset: usize = 0x4
public uartrsr_address: usize = 0x40034004
public uartrsr_named_mask: u32 = 0x0000000f
public uartrsr_reset_mask: u32 = 0x0000000f
public uartrsr_reset_value: u32 = 0x00000000
public uartrsr_image: type = layout(packed, u32) struct
    f_oe: u1 at 3..3
    f_be: u1 at 2..2
    f_pe: u1 at 1..1
    f_fe: u1 at 0..0
end uartrsr_image

public uartrsr_decode: (raw: u32) -> (image: uartrsr_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end uartrsr_decode

public uartrsr_encode: (image: uartrsr_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end uartrsr_encode

public uartrsr_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + uartrsr_offset)
    raw = compiler.register_read(port, compiler.normal_read)
end uartrsr_read

public uartfr_offset: usize = 0x18
public uartfr_address: usize = 0x40034018
public uartfr_named_mask: u32 = 0x000001ff
public uartfr_reset_mask: u32 = 0x000000f8
public uartfr_reset_value: u32 = 0x00000090
public uartfr_image: type = layout(packed, u32) struct
    f_ri: u1 at 8..8
    f_txfe: u1 at 7..7
    f_rxff: u1 at 6..6
    f_txff: u1 at 5..5
    f_rxfe: u1 at 4..4
    f_busy: u1 at 3..3
    f_dcd: u1 at 2..2
    f_dsr: u1 at 1..1
    f_cts: u1 at 0..0
end uartfr_image

public uartfr_decode: (raw: u32) -> (image: uartfr_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end uartfr_decode

public uartfr_encode: (image: uartfr_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end uartfr_encode

public uartfr_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + uartfr_offset)
    raw = compiler.register_read(port, compiler.normal_read)
end uartfr_read

public uartibrd_offset: usize = 0x24
public uartibrd_address: usize = 0x40034024
public uartibrd_named_mask: u32 = 0x0000ffff
public uartibrd_reset_mask: u32 = 0x0000ffff
public uartibrd_reset_value: u32 = 0x00000000
public uartibrd_image: type = layout(packed, u32) struct
    f_baud_divint: u16 at 0..15
end uartibrd_image

public uartibrd_decode: (raw: u32) -> (image: uartibrd_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end uartibrd_decode

public uartibrd_encode: (image: uartibrd_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end uartibrd_encode

public uartibrd_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + uartibrd_offset)
    raw = compiler.register_read(port, compiler.normal_read)
end uartibrd_read

public uartibrd_write: (device_base: usize, raw: u32) -> none =
    port: ptr mut u32 = ptr(device_base + uartibrd_offset)
    compiler.register_write(port, raw, compiler.normal_write,
                            compiler.write_zero, 0x0000ffff)
end uartibrd_write

public uartfbrd_offset: usize = 0x28
public uartfbrd_address: usize = 0x40034028
public uartfbrd_named_mask: u32 = 0x0000003f
public uartfbrd_reset_mask: u32 = 0x0000003f
public uartfbrd_reset_value: u32 = 0x00000000
public uartfbrd_image: type = layout(packed, u32) struct
    f_baud_divfrac: u6 at 0..5
end uartfbrd_image

public uartfbrd_decode: (raw: u32) -> (image: uartfbrd_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end uartfbrd_decode

public uartfbrd_encode: (image: uartfbrd_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end uartfbrd_encode

public uartfbrd_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + uartfbrd_offset)
    raw = compiler.register_read(port, compiler.normal_read)
end uartfbrd_read

public uartfbrd_write: (device_base: usize, raw: u32) -> none =
    port: ptr mut u32 = ptr(device_base + uartfbrd_offset)
    compiler.register_write(port, raw, compiler.normal_write,
                            compiler.write_zero, 0x0000003f)
end uartfbrd_write

public uartlcr_h_offset: usize = 0x2c
public uartlcr_h_address: usize = 0x4003402c
public uartlcr_h_named_mask: u32 = 0x000000ff
public uartlcr_h_reset_mask: u32 = 0x000000ff
public uartlcr_h_reset_value: u32 = 0x00000000
public uartlcr_h_image: type = layout(packed, u32) struct
    f_sps: u1 at 7..7
    f_wlen: u2 at 5..6
    f_fen: u1 at 4..4
    f_stp2: u1 at 3..3
    f_eps: u1 at 2..2
    f_pen: u1 at 1..1
    f_brk: u1 at 0..0
end uartlcr_h_image

public uartlcr_h_decode: (raw: u32) -> (image: uartlcr_h_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end uartlcr_h_decode

public uartlcr_h_encode: (image: uartlcr_h_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end uartlcr_h_encode

public uartlcr_h_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + uartlcr_h_offset)
    raw = compiler.register_read(port, compiler.normal_read)
end uartlcr_h_read

public uartlcr_h_write: (device_base: usize, raw: u32) -> none =
    port: ptr mut u32 = ptr(device_base + uartlcr_h_offset)
    compiler.register_write(port, raw, compiler.normal_write,
                            compiler.write_zero, 0x000000ff)
end uartlcr_h_write

public uarticr_offset: usize = 0x44
public uarticr_address: usize = 0x40034044
public uarticr_named_mask: u32 = 0x000007ff
public uarticr_reset_mask: u32 = 0x00000000
public uarticr_reset_value: u32 = 0x00000000
public uarticr_image: type = layout(packed, u32) struct
    f_oeic: u1 at 10..10
    f_beic: u1 at 9..9
    f_peic: u1 at 8..8
    f_feic: u1 at 7..7
    f_rtic: u1 at 6..6
    f_txic: u1 at 5..5
    f_rxic: u1 at 4..4
    f_dsrmic: u1 at 3..3
    f_dcdmic: u1 at 2..2
    f_ctsmic: u1 at 1..1
    f_rimic: u1 at 0..0
end uarticr_image

public uarticr_decode: (raw: u32) -> (image: uarticr_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end uarticr_decode

public uarticr_encode: (image: uarticr_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end uarticr_encode

public uarticr_write: (device_base: usize, raw: u32) -> none =
    port: ptr mut u32 = ptr(device_base + uarticr_offset)
    compiler.register_write(port, raw, compiler.one_clears,
                            compiler.write_zero, 0x000007ff)
end uarticr_write

public uartdmacr_offset: usize = 0x48
public uartdmacr_address: usize = 0x40034048
public uartdmacr_named_mask: u32 = 0x00000007
public uartdmacr_reset_mask: u32 = 0x00000007
public uartdmacr_reset_value: u32 = 0x00000000
public uartdmacr_image: type = layout(packed, u32) struct
    f_dmaonerr: u1 at 2..2
    f_txdmae: u1 at 1..1
    f_rxdmae: u1 at 0..0
end uartdmacr_image

public uartdmacr_decode: (raw: u32) -> (image: uartdmacr_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end uartdmacr_decode

public uartdmacr_encode: (image: uartdmacr_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end uartdmacr_encode

public uartdmacr_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + uartdmacr_offset)
    raw = compiler.register_read(port, compiler.normal_read)
end uartdmacr_read

public uartdmacr_write: (device_base: usize, raw: u32) -> none =
    port: ptr mut u32 = ptr(device_base + uartdmacr_offset)
    compiler.register_write(port, raw, compiler.normal_write,
                            compiler.write_zero, 0x00000007)
end uartdmacr_write
