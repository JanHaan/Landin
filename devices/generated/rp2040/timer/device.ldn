-- Generated by devices/generate.py from the pinned RP2040 SVD.
-- Copyright (c) 2024 Raspberry Pi Ltd. BSD-3-Clause.
-- See devices/inputs/LICENSE.pico-sdk and devices/README.md.
-- Reset metadata performs no initialization. No device RMW is implicit.
compiler.assert(compiler.byte_order == little)
compiler.assert(sizeof u32 == 4 and alignof u32 == 4)
public base: usize = 0x40054000

public alarm0_offset: usize = 0x10
public alarm0_address: usize = 0x40054010
public alarm0_named_mask: u32 = 0xffffffff
public alarm0_reset_mask: u32 = 0xffffffff
public alarm0_reset_value: u32 = 0x00000000
public alarm0_image: type = layout(packed, u32) struct
    f_alarm0: u32 at 0..31
end alarm0_image

public alarm0_decode: (raw: u32) -> (image: alarm0_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end alarm0_decode

public alarm0_encode: (image: alarm0_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end alarm0_encode

public alarm0_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + alarm0_offset)
    raw = compiler.register_read(port, compiler.normal_read)
end alarm0_read

public alarm0_write: (device_base: usize, raw: u32) -> none =
    port: ptr mut u32 = ptr(device_base + alarm0_offset)
    compiler.register_write(port, raw, compiler.normal_write,
                            compiler.write_zero, 0xffffffff)
end alarm0_write

public alarm1_offset: usize = 0x14
public alarm1_address: usize = 0x40054014
public alarm1_named_mask: u32 = 0xffffffff
public alarm1_reset_mask: u32 = 0xffffffff
public alarm1_reset_value: u32 = 0x00000000
public alarm1_image: type = layout(packed, u32) struct
    f_alarm1: u32 at 0..31
end alarm1_image

public alarm1_decode: (raw: u32) -> (image: alarm1_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end alarm1_decode

public alarm1_encode: (image: alarm1_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end alarm1_encode

public alarm1_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + alarm1_offset)
    raw = compiler.register_read(port, compiler.normal_read)
end alarm1_read

public alarm1_write: (device_base: usize, raw: u32) -> none =
    port: ptr mut u32 = ptr(device_base + alarm1_offset)
    compiler.register_write(port, raw, compiler.normal_write,
                            compiler.write_zero, 0xffffffff)
end alarm1_write

public alarm2_offset: usize = 0x18
public alarm2_address: usize = 0x40054018
public alarm2_named_mask: u32 = 0xffffffff
public alarm2_reset_mask: u32 = 0xffffffff
public alarm2_reset_value: u32 = 0x00000000
public alarm2_image: type = layout(packed, u32) struct
    f_alarm2: u32 at 0..31
end alarm2_image

public alarm2_decode: (raw: u32) -> (image: alarm2_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end alarm2_decode

public alarm2_encode: (image: alarm2_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end alarm2_encode

public alarm2_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + alarm2_offset)
    raw = compiler.register_read(port, compiler.normal_read)
end alarm2_read

public alarm2_write: (device_base: usize, raw: u32) -> none =
    port: ptr mut u32 = ptr(device_base + alarm2_offset)
    compiler.register_write(port, raw, compiler.normal_write,
                            compiler.write_zero, 0xffffffff)
end alarm2_write

public alarm3_offset: usize = 0x1c
public alarm3_address: usize = 0x4005401c
public alarm3_named_mask: u32 = 0xffffffff
public alarm3_reset_mask: u32 = 0xffffffff
public alarm3_reset_value: u32 = 0x00000000
public alarm3_image: type = layout(packed, u32) struct
    f_alarm3: u32 at 0..31
end alarm3_image

public alarm3_decode: (raw: u32) -> (image: alarm3_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end alarm3_decode

public alarm3_encode: (image: alarm3_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end alarm3_encode

public alarm3_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + alarm3_offset)
    raw = compiler.register_read(port, compiler.normal_read)
end alarm3_read

public alarm3_write: (device_base: usize, raw: u32) -> none =
    port: ptr mut u32 = ptr(device_base + alarm3_offset)
    compiler.register_write(port, raw, compiler.normal_write,
                            compiler.write_zero, 0xffffffff)
end alarm3_write

public timerawl_offset: usize = 0x28
public timerawl_address: usize = 0x40054028
public timerawl_named_mask: u32 = 0xffffffff
public timerawl_reset_mask: u32 = 0xffffffff
public timerawl_reset_value: u32 = 0x00000000
public timerawl_image: type = layout(packed, u32) struct
    f_timerawl: u32 at 0..31
end timerawl_image

public timerawl_decode: (raw: u32) -> (image: timerawl_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end timerawl_decode

public timerawl_encode: (image: timerawl_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end timerawl_encode

public timerawl_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + timerawl_offset)
    raw = compiler.register_read(port, compiler.normal_read)
end timerawl_read

public intr_offset: usize = 0x34
public intr_address: usize = 0x40054034
public intr_named_mask: u32 = 0x0000000f
public intr_reset_mask: u32 = 0x0000000f
public intr_reset_value: u32 = 0x00000000
public intr_image: type = layout(packed, u32) struct
    f_alarm_3: u1 at 3..3
    f_alarm_2: u1 at 2..2
    f_alarm_1: u1 at 1..1
    f_alarm_0: u1 at 0..0
end intr_image

public intr_decode: (raw: u32) -> (image: intr_image) =
    image = zeroed
    bits: ptr mut u32 = ptr(usize(addr image))
    bits.val = raw
end intr_decode

public intr_encode: (image: intr_image) -> (raw: u32) =
    bits: ptr u32 = ptr(usize(addr image))
    raw = bits.val
end intr_encode

public intr_read: (device_base: usize) -> (raw: u32) =
    port: ptr u32 = ptr(device_base + intr_offset)
    raw = compiler.register_read(port, compiler.normal_read)
end intr_read

public intr_write: (device_base: usize, raw: u32) -> none =
    port: ptr mut u32 = ptr(device_base + intr_offset)
    compiler.register_write(port, raw, compiler.one_clears,
                            compiler.write_zero, 0x0000000f)
end intr_write
